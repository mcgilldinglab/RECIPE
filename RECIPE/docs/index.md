# RECIPE

RECIPE packages the workflow for bulk protein prediction, PPI refinement, and single-cell transfer into a reusable Python module.

The source package lives in `RECIPE/src/recipe`, while the GitHub repository keeps this package in the `RECIPE/` subdirectory.

```{toctree}
:maxdepth: 2
:caption: Contents

installation
data
script_index
```
